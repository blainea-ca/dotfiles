{{- $name := promptString "name" -}}
{{- $email := promptString "email" -}}
[data]
    name = "{{ $name }}"
    email = "{{ $email }}"

[hooks.read-source-state.pre]
    command = "{{ .chezmoi.sourceDir }}/.chezmoiscripts/install-1password.sh"
